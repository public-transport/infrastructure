apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: public-transport-data-scraper-legacy-proxy
  namespace: app-releases
spec:
  chart:
    spec:
      chart: simple-app
      sourceRef:
        kind: HelmRepository
        name: charts
        namespace: flux-system
      version: "^3.2.2"
  interval: 5m
  values:
    image:
      repositoryWithRegistry: "ghcr.io/juliuste/public-transport-data-scraper-legacy-proxy" # {"$imagepolicy": "flux-system:public-transport-data-scraper-legacy-proxy-image-policy:name"}
      tag: "v1_022720f_2026-07-04T22.38.31Z" # {"$imagepolicy": "flux-system:public-transport-data-scraper-legacy-proxy-image-policy:tag"}
    exposedPort: 3000
    environment:
      PORT: 3000
    hosts:
      - "de.data.public-transport.earth"
    livenessProbe: false
    startupProbe: false
